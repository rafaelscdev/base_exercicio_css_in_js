import { HeroContainer, HeroContent, HeroTitle } from './Hero.styled'

const Hero = () => (
  <HeroContainer>
    <HeroContent className="container">
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </HeroContent>
  </HeroContainer>
)

export default Hero
